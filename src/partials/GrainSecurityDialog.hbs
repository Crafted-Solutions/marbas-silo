{{#> partials/_DialogModal dlgScope='grain-security' dlgClasses='modal-xl'}}
<div class="modal-header">
	<h5 class="modal-title" id="{{dlgScope}}-title">
		<span>{{_ 'Security Options'}}</span>
		<span id="{{dlgScope}}-mod" class="d-none" aria-hidden="true" title="{{_ 'Modified'}}">*</span>
		<div class="fs-6" id="{{dlgScope}}-subtitle"></div>
	</h5>
	{{> partials/_DialogClose}}
</div>
<form class="modal-body table-responsive needs-validation" action="#" onsubmit="return false;" novalidate>
	<table id="{{dlgScope}}-acl" class="table table-bordered table-sm table-striped">
		<caption class="d-none">{{_ 'Access Control List'}}</caption>
		<thead>
			<tr>
				<th scope="col" rowspan="2">
					<div class="btn-group dropend me-2 d-none" id="{{dlgScope}}-add" aria-hidden="true">
						<button type="button" class="btn btn-sm btn-outline-primary dropdown-toggle bi-plus" title="{{_ 'Add entry'}}" data-bs-toggle="dropdown" aria-expanded="false">
							<span class="d-none" id="{{dlgScope}}-add-label">{{_ 'Add entry'}}</span>
						</button>
						<ul class="dropdown-menu" aria-labelledby="{{dlgScope}}-add-label"></ul>
					</div>
					{{_ 'Role'}}
				</th>
				<th colspan="9">{{_ 'Access Control'}}</th>
				<th scope="col" rowspan="2">{{_ 'Iherit'}}</th>
				<th scope="col" rowspan="2">{{_ 'Source'}}</th>
			</tr>
			<tr id="{{dlgScope}}-aclhead" class="th-rotate">
			</tr>
		</thead>
		<tbody></tbody>
	</table>
</form>
{{> partials/_DialogFooter dlgScope=dlgScope}}
<template id="{{dlgScope}}-tpl-addentry">
	<li><button type="button" class="dropdown-item"></button></li>
</template>
<template id="{{dlgScope}}-tpl-aclhead">
	<th scope="col">
		<div class="th-content"></div>
	</th>
</template>
<template id="{{dlgScope}}-tpl-aclentry">
	<tr>
		<td class="{{dlgScope}}-role">
			<button type="button" class="btn btn-sm btn-outline-danger bi-x d-none" title="{{_ 'Delete entry'}}" aria-hidden="true"><span class="d-none">{{_ 'Delete entry'}}</span></button>
			<span class="mb-val"></span>
		</td>
		<td class="{{dlgScope}}-inherit" title="{{_ 'Inhert to children'}}">
			<div class="form-check d-none" aria-hidden="true">
				<input class="form-check-input" type="checkbox" value="1" id="{{dlgScope}}-chk-inherit">
				<label class="form-check-label d-none" for="{{dlgScope}}-chk-inherit">{{_ 'Inhert to children'}}</label>
			</div>
			<span class="mb-val d-none"></span>
		</td>
		<td class="{{dlgScope}}-source"></td>
	</tr>
</template>
<template id="{{dlgScope}}-tpl-aclflag">
	<td>
		<span class="mb-perm-flag text-primary"><span class="d-none mb-val"></span></span>
		<span class="mb-restr-flag text-danger"><span class="d-none mb-val"></span></span>
	</td>
</template>
<template id="{{dlgScope}}-tpl-aclflag-rw">
	<td class="text-nowrap">
		<span class="mb-perm-flag text-nowrap text-primary">
			<input class="btn-check" type="checkbox" value="" id="{{dlgScope}}-chk-perm">
			<label class="btn btn-sm btn-outline-primary bi-hand-thumbs-up-fill" for="{{dlgScope}}-chk-perm" title="{{_ 'Permit'}}"></label>
		</span>
		<span class="mb-restr-flag text-nowrap text-danger">
			<input class="btn-check" type="checkbox" value="" id="{{dlgScope}}-chk-restr">
			<label class="btn btn-sm btn-outline-danger bi-hand-thumbs-down-fill" for="{{dlgScope}}-chk-restr" title="{{_ 'Restrict'}}"></label>
		</span>
	</td>
</template>
{{/partials/_DialogModal}}